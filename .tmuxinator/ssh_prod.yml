---

name: ssh_prod
root: ~/dev/${ORG_ROOT)/src
windows:
    - prod:
        layout: tiled
        panes:
            - ssh bastion
            - ssh prod1
            - ssh prod2
            - ssh pusher
            - ssh science
            - ssh wp
